# Contracts

This folder defines API contracts for internal modules and optional network services.

- `interfaces/` TypeScript interfaces for core domain models
- `schemas/` JSON Schemas for validation
- `openapi.yaml` Optional REST/HTTP API (for mesh/marketplace services)

Principles:
- Local-first by default
- Minimal data collection
- Explicit user consent for microphone and sharing
