{
  "name": "wishbed-app-tdd",
  "version": "1.0.0",
  "description": "WishBed Unified Consciousness Field App - TDD Implementation",
  "main": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:safety": "vitest run --testPathPattern=safety",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
